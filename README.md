Start with making missing directory

`mkdir ./logs,./plugins,./config,./source,./target`

Start Airflow service

`docker compose up -d`

See Airflow UI at

`http://localhost:8080`

username: airflow
pw: airflow
